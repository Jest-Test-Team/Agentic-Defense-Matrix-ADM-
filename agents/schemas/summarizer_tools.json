{
  "type": "function",
  "function": {
    "name": "read_conversation",
    "description": "Read the full conversation history for a given session to generate summaries or extract context.",
    "parameters": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "The session identifier to retrieve conversation history for"
        },
        "include_tool_results": {
          "type": "boolean",
          "description": "Whether to include tool execution results in the history",
          "default": true
        },
        "max_messages": {
          "type": "integer",
          "description": "Maximum number of messages to retrieve. Default is last 100.",
          "default": 100
        }
      },
      "required": ["session_id"]
    }
  }
}
